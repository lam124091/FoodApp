<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@android:color/white" /> <!-- Màu nền của đường viền -->
    <stroke
        android:width="1dp"
        android:color="@android:color/black" /> <!-- Màu của đường viền -->
</shape>
